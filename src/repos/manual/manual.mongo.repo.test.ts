/* Import { beforeEach } from "node:test";

jest.mock('./notes.mongo.model.js');

describe('Given',()=>{
  describe('',()=>{
    let repo:
    const exec = jest.fn().mockresolveedvalue('test')
    beforeEach()
    test(
      const result = await reportError.getAll();
      expect(exec).toHaveBeenCalled();
      expect(result).

    )
  })
}) */
